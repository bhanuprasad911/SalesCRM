{
  "name": "crm",
  "version": "1.0.0",
  "main": "index.js",
  
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend-admin && npm install --prefix frontend-client",
    "build-admin": "npm run build --prefix frontend-admin",
    "build-user": "npm run build --prefix frontend-client",
    "build": "npm run install-all && npm run build-admin && npm run build-user",
    "start": "npm run dev --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
